name: al_shalla_app
description: "تطبيق الشلة - مسنجر وبنترست الذكي"
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.7.0
  shared_preferences: ^2.2.2
  image_picker: ^1.0.7
  record: ^5.1.2
  audioplayers: ^6.0.0
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
